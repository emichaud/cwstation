# Help System Configuration
# This file defines YOUR project's documentation structure
# SmallStack docs are bundled separately and auto-injected when enabled

title: "CW Station — Documentation"

# Variables available in markdown files using {{ variable }} syntax
variables:
  version: "1.0.0"
  project_name: "CW Station"
  python_version: "3.12"
  django_version: "6.0"

# CW Station documentation
sections:
  - slug: ""
    title: "CW Station"
    description: "Decode, practice, send, and log CW"
    pages:
      - slug: index
        title: "CW Station"
        description: "What CW Station is and where to start"
        icon: "radio"
      - slug: cw-tutorial
        title: "Tutorial: Your First Ten Minutes"
        description: "Step-by-step from first decode to working a simulated band"
        icon: "rocket"
      - slug: cw-station
        title: "Operator's Guide"
        description: "Decode CW off the air, practice, send — and replay it on the tape"
        icon: "book-open"
      - slug: cw-install
        title: "Install & Deploy"
        description: "Dependencies, config, and the two production gotchas"
        icon: "server"
      - slug: morse-reference
        title: "Morse Code Reference"
        description: "Every letter, number, punctuation mark, and prosign the station speaks"
        icon: "book"

# SmallStack's framework reference docs are bundled in apps/help/smallstack/.
# They are tucked away for CW Station: SMALLSTACK_DOCS_ENABLED defaults to False
# (config/settings/smallstack.py). Set it True in the environment to expose the
# framework reference again while developing.
